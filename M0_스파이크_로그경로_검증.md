# M0 스파이크 — Windows CLI 로그 경로/포맷 실측 검증

> 검증일: 2026-07-22 · 검증 머신: Windows 11 Pro (`C:\Users\nhn`)
> 목적: 계획서 §3 "선행 검증 필요" 항목 — Windows에서 CLI들이 실제로 어느 경로에 어떤 포맷 JSONL을 남기는지 실기 확인.
> 이게 어긋나면 앱 전체가 "사용량 0"이 되므로 착수 전 최우선 검증 항목.

## 착수 결정 (확정)

- **트레이 표시**: 동적 아이콘 + 툴팁 (계획서 §4 선택지 1번)
- **프런트엔드**: React
- **시작 지점**: M0 스파이크 (본 문서)

---

## 검증 결과 요약

| CLI | 계획서 가정 경로 | 실측 결과 | 판정 |
|---|---|---|---|
| Claude | `%USERPROFILE%\.claude\projects` | 존재, JSONL 57개, 포맷 일치 | ✅ 검증 완료 |
| Codex | `%USERPROFILE%\.codex\sessions` | 존재, JSONL 22개, 날짜 중첩 구조 | ✅ 검증 완료 |
| Gemini | `%USERPROFILE%\.gemini\tmp\<hash>\chats\session-*.jsonl` | **`tmp` 없음 — Gemini CLI 미설치(Antigravity IDE만)** | ⚠️ 라이브 데이터 미검증 (경로 자체는 원본 코드로 확인됨) |

경로 접두사(`%USERPROFILE%`)는 세 CLI 모두 일치. `%APPDATA%`/`%LOCALAPPDATA%`로 새는 케이스는 확인되지 않음 → 계획서 경로 매핑 표의 로그 경로는 유효.

> **원본 소스 대조 완료** (`upstream/Sources/PokeTokenBar/Core/LocalUsageReader.swift`):
> - Claude: `~/.claude/projects/**/*.jsonl` — `type:"assistant"` 라인
> - Codex: `~/.codex/sessions/**/rollout-*.jsonl` — `event_msg.payload.type:"token_count"`
> - Gemini: `~/.gemini/tmp/<hash>/chats/session-*.jsonl` (+ 레거시 `.json`), 세션 객체에 `startTime`/`messages`
> 세 경로 모두 `homeDirectoryForCurrentUser` 기준 → Rust에서 `dirs::home_dir()`로 동일 매핑.

---

## 1. Claude — ✅ 검증 완료

- **경로**: `C:\Users\nhn\.claude\projects\<프로젝트슬러그>\<sessionId>.jsonl`
  - 서브에이전트 로그는 `.../<sessionId>/subagents/agent-*.jsonl`에 별도 존재 (사용량 집계 시 포함 여부 결정 필요)
  - 프로젝트 슬러그는 cwd 경로를 `-`로 치환한 형태 (예: `c--00-SVN-Match3Prototype`)
- **사용량 필드**: `type:"assistant"` 라인의 `message.usage`
  ```json
  "usage":{"input_tokens":8894,"cache_creation_input_tokens":12969,"cache_read_input_tokens":21294,"output_tokens":252,"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0}}
  ```
- **모델 필드**: `message.model` (예: `claude-opus-4-8`, 합성 메시지는 `<synthetic>` → 집계 제외 필요)
- **파서 유의점**:
  - 첫 줄들은 `queue-operation` 등 메타 이벤트 → `type` 분기 필수
  - 캐시 토큰(`cache_creation`/`cache_read`)이 별도 필드 → 비용 계산 시 단가 구분 (ModelPricing 테이블에 캐시 단가 반영 확인)
  - `<synthetic>` 모델 라인 제외

## 2. Codex — ✅ 검증 완료

- **경로**: `C:\Users\nhn\.codex\sessions\YYYY\MM\DD\rollout-<ISO시각>-<uuid>.jsonl` (날짜 3단 중첩)
- **세션 메타**: 첫 줄 `type:"session_meta"` → `cwd`, `cli_version`, `model_provider` 등
- **사용량 필드**: `type:"token_count"` 이벤트
  ```json
  {"type":"token_count","info":{
    "total_token_usage":{"input_tokens":13101,"cached_input_tokens":2432,"output_tokens":200,"reasoning_output_tokens":94,"total_tokens":13301},
    "last_token_usage":{...},
    "model_context_window":258400},
   "rate_limits":{"primary":{"used_percent":1.0,"window_minutes":300,"resets_at":1783337576},"secondary":{...},"plan_type":"team"}}
  ```
- **파서 유의점**:
  - `total_token_usage`는 세션 **누적**, `last_token_usage`는 마지막 턴 → 집계 방식(마지막 total만 취할지, 델타 합산할지) 결정 필요
  - `reasoning_output_tokens` 별도 필드 → output 비용에 포함할지 정책 결정
  - `rate_limits`는 계획서 `CodexRateLimitsProvider`(공식 한도)에 그대로 매핑 가능 — `used_percent`/`window_minutes`/`resets_at`(epoch)

## 3. Gemini — ⚠️ 라이브 데이터 미검증 (경로는 확인됨)

- **원본 경로**: `~/.gemini/tmp/<hash>/chats/session-*.jsonl` (+ 레거시 `.json`) — `LocalUsageReader.swift:45,178` 확인.
- **이 머신엔 `.gemini\tmp`가 없음** → 경로 오류가 아니라 **Gemini CLI 미설치** 때문.
  - `.gemini`에는 **Antigravity IDE** 데이터만 존재 (`antigravity/brain/...`, `settings.json`).
  - `gemini` CLI 바이너리 PATH 부재 확인.
- **세션 파싱 구조**(원본): 세션 객체에 `startTime`(ISO8601) + `messages[]` → 메시지별 usage 흡수.

### 결론 및 액션
- 계획서/원본의 Gemini 경로 가정은 **정확**. 남은 것은 실제 CLI가 깔린 환경에서 라이브 데이터 포맷 최종 확인뿐.
- **권장**: Claude/Codex는 즉시 M1 진행. Gemini는 파서를 원본 스펙대로 구현해두되, 런타임에 `~/.gemini/tmp` 부재 시 **우아하게 숨김**(원본의 프로바이더별 opt-out 패턴). Gemini CLI 설치 머신 확보 시 라이브 검증.

---

## M0 → M1 인계 사항

1. Rust 파서는 **CLI별 별도 모듈** + 공통 `UsageProvider` trait로 (계획서 §2 구조 그대로 유효).
2. Claude/Codex 파서는 위 실측 포맷 그대로 스펙 확정 → M1 착수 가능.
3. **경로 상수는 `%USERPROFILE%` 기준 하드코딩 금지** — `dirs`/`directories` crate로 홈 디렉토리 해석.
4. Gemini는 feature-flag 또는 런타임 감지로 미설치 시 우아하게 숨김.
5. 캐시 토큰·reasoning 토큰의 비용 집계 정책은 M1 착수 시 원본 `ModelPricing.swift`/`UsageStore.swift` 로직 대조 후 확정 (원본 소스 확보 필요).

## 남은 선행 과제
- **원본 소스(`chattymin/PokeTokenBar`) 확보**: 게임 규칙·비용 로직·테스트 19개가 M1의 "정답지". 아직 이 폴더에 없음.
- **Tauri 트레이 PoC**: 동적 아이콘 갱신(타이머로 아이콘 이미지 교체) 실현성 확인 — M0 잔여 항목.
