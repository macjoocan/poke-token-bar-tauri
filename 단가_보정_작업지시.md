# 단가 보정 작업 지시 — 비용이 35.6% 과소집계되고 있다

> 작성: 2026-09-07 · 실측 머신: Windows 11 (`C:\Users\NHN`)
> 계기: `cc-launcher` 프로젝트에 토큰 사용량 표시를 붙이면서 이 프로젝트의 `model_pricing.rs` 를
> 참고했고, 그 과정에서 단가 테이블이 낡은 것을 발견했다.
> 대상 파일: `poketokenbar/src-tauri/src/core/model_pricing.rs`, `local_usage_reader.rs`

## 결론

로컬 Claude 로그 전체(중복 제거 후 17,026건)로 실측한 결과, 현재 앱이 계산하는 비용이
공식 단가 대비 **$2,750.87 (35.6%) 적다.**

| 모델 | 현재 앱 | 공식 단가 | 차이 |
|---|---:|---:|---:|
| claude-opus-5 | $3,911.90 | $4,431.20 | +$519.30 |
| claude-fable-5 | **$0.00** | $2,012.04 | **+$2,012.04** |
| claude-opus-4-8 | $1,063.06 | $1,253.17 | +$190.11 |
| claude-fable-5-1 | **$0.00** | $29.42 | +$29.42 |
| claude-haiku-4-5-20251001 | $0.07 | $0.07 | ±0 |
| **합계** | **$4,975.03** | **$7,725.90** | **+$2,750.87** |

재현: `단가_보정_실측.js` (이 폴더) → `node 단가_보정_실측.js`

### 실측된 모델 구성 (중복 제거 후, 로그 전체)

| 모델 | 응답 | 입력 | 출력 | 캐시쓰기 | 캐시읽기 | 1h | 5m |
|---|---:|---:|---:|---:|---:|---:|---:|
| claude-opus-5 | 11,848 | 0.03M | 11.75M | 140.04M | 5,484.70M | 138.46M | 1.58M |
| claude-opus-4-8 | 2,801 | 0.06M | 4.88M | 51.57M | 1,236.58M | 50.70M | 0.88M |
| claude-fable-5 | 2,161 | 0.03M | 2.38M | 45.25M | 992.36M | 44.63M | 0.62M |
| claude-fable-5-1 | 171 | 0.02M | 0.20M | 0.41M | 43.89M | 0.41M | 0.00M |
| claude-haiku-4-5-20251001 | 7 | ~0 | ~0 | 0.03M | 0.13M | 0.00M | 0.03M |
| `<synthetic>` | 38 | 0 | 0 | 0 | 0 | — | — |

두 가지가 눈에 걸린다:

- **Fable 계열이 전체의 13%** 인데 $0 으로 잡힌다
- **캐시 쓰기의 98.9% 가 1시간 캐시**다 (`ephemeral_1h`). 지금 앱은 이걸 5분 단가로 계산한다

---

## 결함 1 — Fable 이 통째로 $0 (영향 −$2,041, 가장 큼)

**위치**: `model_pricing.rs:35`

```rust
"claude-fable-5" => Some(ModelRate::ZERO), // ccusage 미가격 → $0
```

**왜 이렇게 됐나**: 표를 만들 당시 ccusage 의 오프라인 LiteLLM 스냅샷에 Fable 단가가
없어서 $0 으로 맞춘 것이다. 그때는 맞는 선택이었다. 지금 Fable 5 는 공식 가격표에
**$10 / $50** 로 올라와 있다.

**`claude-fable-5-1` 은 더 조용히 틀린다.** 정확 매칭 표에 없고, 패밀리 폴백
(`opus` / `sonnet` / `haiku` / `gpt` / `gemini`) 어디에도 안 걸려서 마지막 줄
`ModelRate::ZERO` 로 떨어진다. 로그에 171건 있다.

**주의할 특수 케이스**: Fable 5.1 · Mythos 5.1 의 캐시 읽기는 **0.025배 ($0.25)** 다.
다른 모델의 0.1배와 다르다. 공식 문서 각주에 명시돼 있다.

---

## 결함 2 — 1시간 캐시 쓰기를 5분 단가로 계산 (영향 −$710)

**위치**: `model_pricing.rs` 의 `ModelRate` 구조체, `local_usage_reader.rs:168`

`ModelRate` 에 `cache_write` 필드가 **하나**뿐이고, 거기에 5분 단가(기본 입력의 1.25배)가
들어 있다. 그런데 1시간 캐시 쓰기는 **2배**다. Opus 기준 $6.25 vs $10 — 60% 차이.

그리고 파서가 아예 그 정보를 읽지 않는다:

```rust
// local_usage_reader.rs:168
cache_write: int_value(usage.get("cache_creation_input_tokens")),
```

`cache_creation.ephemeral_1h_input_tokens` / `ephemeral_5m_input_tokens` 문자열은
프로젝트 전체에 하나도 없다 (`grep -rn "ephemeral" src/` → 0건).

실제 로그에는 둘 다 들어 있다:

```json
"usage": {
  "cache_creation_input_tokens": 12345,
  "cache_creation": {
    "ephemeral_1h_input_tokens": 12000,
    "ephemeral_5m_input_tokens": 345
  }
}
```

캐시 쓰기 비용만 따로 보면: **$1,197.72 → $1,915.72**

---

## 결함 3 — `claude-opus-5` 가 표에 없다 (금액 영향 0, 그러나 운에 기대는 상태)

정확 매칭에 없어서 `m.contains("opus")` 폴백이 `5/25/6.25/0.5` 를 준다. 이게
**우연히 Opus 5 실제 단가와 일치**한다 (Opus 4.5 ~ 5 가 모두 $5/$25). 지금은 안 틀렸다.

**같이 심어져 있는 시한폭탄**: `claude-sonnet-5` 는 **$2 / $10** 인데, sonnet 폴백은
$3 / $15 를 준다. Sonnet 5 를 쓰는 순간 **50% 과대계상**된다. 현재 로그에는 없지만
Claude Code 의 기본 모델이 바뀌면 바로 터진다.

---

## 공식 단가 (2026-09-07 확인)

출처: <https://platform.claude.com/docs/en/about-claude/pricing> (USD / Mtok)

| 모델 | 입력 | 5분 캐시쓰기 | 1시간 캐시쓰기 | 캐시읽기 | 출력 |
|---|---:|---:|---:|---:|---:|
| Claude Fable 5.1 | 10 | 12.50 | 20 | **0.25** | 50 |
| Claude Mythos 5.1 | 10 | 12.50 | 20 | **0.25** | 50 |
| Claude Fable 5 | 10 | 12.50 | 20 | 1 | 50 |
| Claude Mythos 5 | 10 | 12.50 | 20 | 1 | 50 |
| Claude Opus 5 | 5 | 6.25 | 10 | 0.50 | 25 |
| Claude Opus 4.8 | 5 | 6.25 | 10 | 0.50 | 25 |
| Claude Opus 4.7 | 5 | 6.25 | 10 | 0.50 | 25 |
| Claude Opus 4.6 | 5 | 6.25 | 10 | 0.50 | 25 |
| Claude Opus 4.5 | 5 | 6.25 | 10 | 0.50 | 25 |
| Claude Opus 4.1 (은퇴) | 15 | 18.75 | 30 | 1.50 | 75 |
| Claude Opus 4 (은퇴) | 15 | 18.75 | 30 | 1.50 | 75 |
| **Claude Sonnet 5** | **2** | **2.50** | **4** | **0.20** | **10** |
| Claude Sonnet 4.6 | 3 | 3.75 | 6 | 0.30 | 15 |
| Claude Sonnet 4.5 | 3 | 3.75 | 6 | 0.30 | 15 |
| Claude Sonnet 4 (은퇴) | 3 | 3.75 | 6 | 0.30 | 15 |
| Claude Haiku 4.5 | 1 | 1.25 | 2 | 0.10 | 5 |
| Claude Haiku 3.5 (은퇴) | 0.80 | 1 | 1.60 | 0.08 | 4 |

배수 규칙 (기본 입력 단가 대비):

- 5분 캐시 쓰기 = **1.25배**
- 1시간 캐시 쓰기 = **2배**
- 캐시 읽기 = **0.1배** — 단, Fable 5.1 · Mythos 5.1 은 **0.025배**

Sonnet 5 관련 문서 각주: 출시 시 "2026-08-31 까지 도입가"로 안내됐던 $2/$10 이
**정식 단가로 확정**됐다. 2026-09-01 예정이던 $3/$15 인상은 없다.

---

## 수정 계획

### 1) `model_pricing.rs` — `ModelRate` 에 1h/5m 분리

```rust
pub struct ModelRate {
    pub input: f64,
    pub output: f64,
    pub cache_write_5m: f64,   // 기존 cache_write 를 개명
    pub cache_write_1h: f64,   // 신규
    pub cache_read: f64,
}
```

`per_million()` 인자도 5개로 늘린다. `ZERO` 상수 유지.

### 2) `model_pricing.rs::rate()` — 표 갱신

정확 매칭에 추가/수정할 것:

- `claude-opus-5` 추가 (폴백 의존 제거)
- `claude-fable-5` → `ZERO` 를 **실제 단가로 교체** `(10, 50, 12.5, 20, 1)`
- `claude-fable-5-1` 추가 `(10, 50, 12.5, 20, 0.25)` ← 캐시읽기 0.25 주의
- `claude-mythos-5` / `claude-mythos-5-1` 추가 (Fable 과 동일)
- `claude-sonnet-5` 추가 `(2, 10, 2.5, 4, 0.2)` ← **폴백과 다르다**
- `claude-opus-4-6` / `claude-opus-4-5` 추가
- `claude-sonnet-4-5` 추가
- `claude-haiku-4-5` 를 접두 매칭으로 (날짜 접미사 드리프트 대비)

패밀리 폴백 정리:

- `fable` / `mythos` 폴백 신설 → `(10, 50, 12.5, 20, 1)`
  (5.1 계열의 0.025배는 정확 매칭에서만 처리. 폴백은 보수적으로 1 을 쓴다)
- `opus` 폴백에 1h 단가 10 추가
- `sonnet` 폴백은 4.6 기준($3/$15) 유지. **Sonnet 5 는 정확 매칭으로 잡는다**
- `haiku` 폴백에 1h 단가 2 추가

### 3) `local_usage_reader.rs` — 1h/5m 를 읽어서 나른다

`Entry` (line 27) / `Bucket` (line 45):

```rust
pub cache_write_5m: i64,
pub cache_write_1h: i64,
```

`Entry::total()` (line 39) 과 `Bucket::total()` (line 54) 의 합에 두 필드를 쓴다.
기존 `cache_write` 필드는 **없앤다** — 남겨두면 이중 계산 사고가 난다.
표시용 합계가 필요한 자리에는 `cache_write()` 헬퍼 메서드를 둔다.

파싱 (line 161~170, Claude 경로):

```rust
let cw_total = int_value(usage.get("cache_creation_input_tokens"));
let cc = usage.get("cache_creation");
let w1h = cc.map(|c| int_value(c.get("ephemeral_1h_input_tokens"))).unwrap_or(0);
let w5m = cc.map(|c| int_value(c.get("ephemeral_5m_input_tokens"))).unwrap_or(0);
// 구버전 로그 호환: cache_creation 이 없거나 합이 모자라면 잔여분은 5분으로 본다
let rest = (cw_total - w1h - w5m).max(0);
// → cache_write_1h: w1h, cache_write_5m: w5m + rest
```

**이 폴백 규칙이 중요하다.** `cache_creation` 객체가 없는 옛 로그는 전부 5분 단가로
계산되어 기존 동작과 같아진다. 즉 이 변경으로 과거 수치가 임의로 튀지 않는다.

`Bucket::add()` (line 58~63):

```rust
self.cost += model_pricing::cost(
    &e.model, e.input, e.output, e.cache_write_5m, e.cache_write_1h, e.cache_read);
```

`cost()` 인자 5개 → 6개.

**Entry 생성 지점 4곳 전부 손봐야 한다** (컴파일러가 다 잡아준다):

| 위치 | 무엇 |
|---|---|
| `local_usage_reader.rs:161` | Claude 파싱 — 위 로직 적용 |
| `local_usage_reader.rs:225` | Codex 파싱 — Codex 는 캐시쓰기가 없다. 둘 다 `0` |
| `local_usage_reader.rs:340` | 테스트 픽스처 |
| `local_usage_reader.rs:604` | 테스트 픽스처 |

### 4) 프론트엔드는 손댈 필요 없다

`models.rs:16` 의 `cache_creation_tokens` 는 그대로 두고 `local_usage_reader.rs:434` 에서
`b.cache_write_5m + b.cache_write_1h` 를 넣으면 된다.
`App.tsx:687` 의 "캐시 쓰기" 행이 계속 합계를 보여준다.

1h/5m 를 UI 에 나눠 보여주고 싶으면 `BlockUsage` / `DailyUsage` 에 필드를 더하는
별도 작업으로 한다. **이번 작업 범위 밖이다** — 이번 건은 금액 정확도만 고친다.

### 5) 헤더 주석 갱신 (`model_pricing.rs:1-2`)

```rust
//! ccusage(--offline, LiteLLM 스냅샷) 단가를 실측 역산해 동일하게 번들 (적합오차 0.000%).
```

이 문장이 이제 **문제의 원인**이다. ccusage 의 오프라인 스냅샷이 낡았고, 거기 맞추면
그 낡음을 그대로 물려받는다. 기준을 공식 가격표로 바꾸고, 확인 날짜와 출처 URL 을 적는다.

---

## 테스트

`model_pricing.rs` 의 `pricing_exact_and_fallback_and_zero` (line 80~) 는 기대값이
바뀌므로 그대로는 깨진다. 갱신 + 아래 케이스 추가:

```
1) Fable 5 가 더 이상 $0 이 아니다
   cost("claude-fable-5", 1M, 0, 0, 0, 0)      == 10.0
   cost("claude-fable-5", 0, 1M, 0, 0, 0)      == 50.0
   cost("claude-fable-5", 0, 0, 0, 0, 1M)      ==  1.0   (캐시읽기)

2) Fable 5.1 캐시읽기는 0.025배
   cost("claude-fable-5-1", 0, 0, 0, 0, 1M)    ==  0.25

3) 1h 와 5m 이 다른 단가로 계산된다
   cost("claude-opus-5", 0, 0, 1M, 0, 0)       ==  6.25   (5m)
   cost("claude-opus-5", 0, 0, 0, 1M, 0)       == 10.0    (1h)

4) Sonnet 5 는 폴백($3/$15)이 아니라 정확 매칭($2/$10)을 탄다
   cost("claude-sonnet-5", 1M, 0, 0, 0, 0)     ==  2.0
   cost("claude-sonnet-4-6", 1M, 0, 0, 0, 0)   ==  3.0

5) 미지 모델은 여전히 $0 (오표시 방지)
   cost("totally-unknown", 1M, 1M, 1M, 1M, 1M) ==  0.0

6) opus 패밀리 폴백에 1h 가 들어갔다
   cost("claude-opus-4-99", 0, 0, 0, 1M, 0)    == 10.0
```

`local_usage_reader.rs` 파싱 테스트 추가:

```
7) cache_creation 이 있으면 1h/5m 로 갈라진다
8) cache_creation 이 없는 옛 로그는 전부 5m 으로 간다 (기존 동작 보존)
9) cache_creation 합이 cache_creation_input_tokens 보다 작으면 잔여분은 5m
```

기존 픽스처 생성기(line 537 근처의 `format!`)에 `cache_creation` 을 넣는 변형이 필요하다.

---

## 검증 방법

```powershell
cd poketokenbar/src-tauri
cargo test
cargo build --release
```

빌드 후 앱 수치를 이 문서의 실측표와 맞춰본다:

```powershell
node ../../단가_보정_실측.js
```

이 스크립트의 "공식 단가" 열이 **고친 앱과 일치해야 한다**.
현재 상태로 돌리면 "현재 앱" 열이 지금 앱 수치와 일치하는 것으로
스크립트 자체의 정확성을 먼저 확인할 수 있다 (되돌림 검증).

주의: 스크립트는 실행 시점의 로그 전체를 읽으므로, 위 표의 금액은 계속 커진다.
**절대 금액이 아니라 두 열의 일치 여부**를 본다.

---

## 건드리지 않을 것

`gpt-5.5` 와 `gemini-*` 항목은 이번에 확인하지 않았다. Anthropic 문서 밖이라
OpenAI · Google 공식 가격표를 따로 확인해야 한다. **모르는 채로 손대면 안 된다.**
지금 값을 그대로 두고, 확인은 별도 작업으로 남긴다.

`claude-opus-4-7` 은 표에 있고 값도 맞다 ($5/$25). 1h 단가만 추가하면 된다.

---

## 왜 이게 조용히 틀렸나 (재발 방지)

세 결함이 다 **"틀린 값을 자신 있게 보여주는"** 형태다. 에러도 경고도 안 난다.

- Fable → `ZERO` 는 "미가격 모델은 0" 이라는 ccusage 규칙을 따른 것이었다.
  모델이 가격표에 올라오는 순간 그 규칙이 거짓말이 된다
- 1h/5m → 필드가 하나뿐이라 **구조적으로 표현할 수 없었다.** 값을 고치는 문제가 아니었다
- 폴백 → `contains("opus")` 는 새 모델을 조용히 흡수한다. 단가가 같으면 맞고,
  다르면 틀린다. 어느 쪽인지 알려주지 않는다

**제안**: 정확 매칭에 없는 모델명을 만나면 로그를 한 줄 남긴다. 폴백이 동작한 것 자체는
정상이지만, 그게 조용하면 안 된다. 새 모델이 나올 때 사람이 알아채는 유일한 지점이다.
